{% extends "layouts/page-full-width.njk" %}

{% set pageName = "Buttons" %}

{% set examples = [
  { name: "Arrow up-down", modifier: "arrow-up-down" },
  null,
  null,
  null,
  { name: "Arrow left", modifier: "arrow-left" },
  { name: "Arrow right", modifier: "arrow-right" },
  { name: "Arrow up", modifier: "arrow-up" },
  { name: "Arrow down", modifier: "arrow-down" },
  { name: "Arrow left circle", modifier: "arrow-left-circle" },
  { name: "Arrow right circle", modifier: "arrow-right-circle" },
  { name: "Arrow up circle", modifier: "arrow-up-circle" },
  { name: "Arrow down circle", modifier: "arrow-down-circle" },
  { name: "Chevron left circle", modifier: "chevron-left-circle" },
  { name: "Chevron right circle", modifier: "chevron-right-circle" },
  { name: "Chevron up circle", modifier: "chevron-up-circle" },
  { name: "Chevron down circle", modifier: "chevron-down-circle" },
  { name: "Cross", modifier: "cross" },
  { name: "Tick", modifier: "tick" },
  { name: "Minus", modifier: "minus" },
  { name: "Plus", modifier: "plus" },
  { name: "Search", modifier: "search" },
  { name: "User", modifier: "user" },
  { name: "Copy", modifier: "copy" }
] %}

{% block beforeContent %}
  {{ breadcrumb({
    items: [
      {
        text: "NHS.UK frontend",
        href: baseUrl
      },
      {
        text: "Examples",
        href: baseUrl ~ "#examples"
      }
    ]
  }) }}
{% endblock %}

{% block content %}
  <section class="nhsuk-main-wrapper nhsuk-main-wrapper--s">
    <div class="nhsuk-width-container">

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-two-thirds">
          <h1 class="nhsuk-heading-xl">{{ pageName }}</h1>
          <h2 class="nhsuk-heading-l nhsuk-u-margin-bottom-0">Page background</h2>
        </div>
      </div>

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-one-third">
          <p class="nhsuk-body nhsuk-u-margin-top-6">Button element</p>

          <div class="nhsuk-button-group app-button-group">
            {{ button({
              text: "Save and continue"
            }) }}

            {{ button({
              text: "Yes, delete this vaccine",
              variant: "warning"
            }) }}

            {{ button({
              text: "Continue to NHS login",
              variant: "login"
            }) }}

            {{ button({
              text: "Find my location",
              variant: "secondary"
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <p class="nhsuk-body nhsuk-u-margin-top-6">Link element</p>

          <div class="nhsuk-button-group app-button-group">
            {{ button({
              text: "Save and continue",
              href: "#"
            }) }}

            {{ button({
              text: "Yes, delete this vaccine",
              variant: "warning",
              href: "#"
            }) }}

            {{ button({
              text: "Continue to NHS login",
              variant: "login",
              href: "#"
            }) }}

            {{ button({
              text: "Find my location",
              variant: "secondary",
              href: "#"
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <p class="nhsuk-body nhsuk-u-margin-top-6">Disabled</p>

          <div class="nhsuk-button-group app-button-group">
            {{ button({
              text: "Save and continue",
              disabled: true
            }) }}

            {{ button({
              text: "Yes, delete this vaccine",
              variant: "warning",
              disabled: true
            }) }}

            {{ button({
              text: "Continue to NHS login",
              variant: "login",
              disabled: true
            }) }}

            {{ button({
              text: "Find my location",
              variant: "secondary",
              disabled: true
            }) }}
          </div>
        </div>
      </div>

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          <div class="nhsuk-button-group">
            {{ button({
              text: "Save and continue",
              small: true
            }) }}

            {{ button({
              text: "Yes, delete this vaccine",
              variant: "warning",
              small: true
            }) }}

            {{ button({
              text: "Continue to NHS login",
              variant: "login",
              small: true
            }) }}

            {{ button({
              text: "Find my location",
              variant: "secondary",
              small: true
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          <div class="nhsuk-button-group">
            {{ button({
              text: "Save and continue",
              small: true,
              href: "#"
            }) }}

            {{ button({
              text: "Yes, delete this vaccine",
              variant: "warning",
              small: true,
              href: "#"
            }) }}

            {{ button({
              text: "Continue to NHS login",
              variant: "login",
              small: true,
              href: "#"
            }) }}

            {{ button({
              text: "Find my location",
              variant: "secondary",
              small: true,
              href: "#"
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          <div class="nhsuk-button-group">
            {{ button({
              text: "Save and continue",
              small: true,
              disabled: true
            }) }}

            {{ button({
              text: "Yes, delete this vaccine",
              variant: "warning",
              small: true,
              disabled: true
            }) }}

            {{ button({
              text: "Continue to NHS login",
              variant: "login",
              small: true,
              disabled: true
            }) }}

            {{ button({
              text: "Find my location",
              variant: "secondary",
              small: true,
              disabled: true
            }) }}
          </div>
        </div>
      </div>

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              icon: item.modifier,
              small: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "warning",
              icon: item.modifier,
              small: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "login",
              icon: item.modifier,
              small: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "secondary",
              icon: item.modifier,
              small: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              icon: item.modifier,
              small: true,
              href: "#"
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "warning",
              icon: item.modifier,
              small: true,
              href: "#"
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "login",
              icon: item.modifier,
              small: true,
              href: "#"
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "secondary",
              icon: item.modifier,
              small: true,
              href: "#"
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              icon: item.modifier,
              small: true,
              disabled: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "warning",
              icon: item.modifier,
              small: true,
              disabled: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "login",
              icon: item.modifier,
              small: true,
              disabled: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "secondary",
              icon: item.modifier,
              small: true,
              disabled: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </section>

  <section class="nhsuk-main-wrapper app-wrapper app-wrapper--background-white">
    <div class="nhsuk-width-container">

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-two-thirds">
          <h2 class="nhsuk-heading-l nhsuk-u-margin-bottom-0">White background</h2>
        </div>
      </div>

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-one-third">
          <p class="nhsuk-body nhsuk-u-margin-top-6">Button element</p>

          <div class="nhsuk-button-group app-button-group">
            {{ button({
              text: "Save and continue"
            }) }}

            {{ button({
              text: "Yes, delete this vaccine",
              variant: "warning"
            }) }}

            {{ button({
              text: "Continue to NHS login",
              variant: "login"
            }) }}

            {{ button({
              text: "Find my location",
              variant: "secondary"
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <p class="nhsuk-body nhsuk-u-margin-top-6">Link element</p>

          <div class="nhsuk-button-group app-button-group">
            {{ button({
              text: "Save and continue",
              href: "#"
            }) }}

            {{ button({
              text: "Yes, delete this vaccine",
              variant: "warning",
              href: "#"
            }) }}

            {{ button({
              text: "Continue to NHS login",
              variant: "login",
              href: "#"
            }) }}

            {{ button({
              text: "Find my location",
              variant: "secondary",
              href: "#"
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <p class="nhsuk-body nhsuk-u-margin-top-6">Disabled</p>

          <div class="nhsuk-button-group app-button-group">
            {{ button({
              text: "Save and continue",
              disabled: true
            }) }}

            {{ button({
              text: "Yes, delete this vaccine",
              variant: "warning",
              disabled: true
            }) }}

            {{ button({
              text: "Continue to NHS login",
              variant: "login",
              disabled: true
            }) }}

            {{ button({
              text: "Find my location",
              variant: "secondary",
              disabled: true
            }) }}
          </div>
        </div>
      </div>

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          <div class="nhsuk-button-group">
            {{ button({
              text: "Save and continue",
              small: true
            }) }}

            {{ button({
              text: "Yes, delete this vaccine",
              variant: "warning",
              small: true
            }) }}

            {{ button({
              text: "Continue to NHS login",
              variant: "login",
              small: true
            }) }}

            {{ button({
              text: "Find my location",
              variant: "secondary",
              small: true
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          <div class="nhsuk-button-group">
            {{ button({
              text: "Save and continue",
              small: true,
              href: "#"
            }) }}

            {{ button({
              text: "Yes, delete this vaccine",
              variant: "warning",
              small: true,
              href: "#"
            }) }}

            {{ button({
              text: "Continue to NHS login",
              variant: "login",
              small: true,
              href: "#"
            }) }}

            {{ button({
              text: "Find my location",
              variant: "secondary",
              small: true,
              href: "#"
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          <div class="nhsuk-button-group">
            {{ button({
              text: "Save and continue",
              small: true,
              disabled: true
            }) }}

            {{ button({
              text: "Yes, delete this vaccine",
              variant: "warning",
              small: true,
              disabled: true
            }) }}

            {{ button({
              text: "Continue to NHS login",
              variant: "login",
              small: true,
              disabled: true
            }) }}

            {{ button({
              text: "Find my location",
              variant: "secondary",
              small: true,
              disabled: true
            }) }}
          </div>
        </div>
      </div>

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              icon: item.modifier,
              small: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "warning",
              icon: item.modifier,
              small: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "login",
              icon: item.modifier,
              small: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "secondary",
              icon: item.modifier,
              small: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              icon: item.modifier,
              small: true,
              href: "#"
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "warning",
              icon: item.modifier,
              small: true,
              href: "#"
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "login",
              icon: item.modifier,
              small: true,
              href: "#"
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "secondary",
              icon: item.modifier,
              small: true,
              href: "#"
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              icon: item.modifier,
              small: true,
              disabled: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "warning",
              icon: item.modifier,
              small: true,
              disabled: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "login",
              icon: item.modifier,
              small: true,
              disabled: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "secondary",
              icon: item.modifier,
              small: true,
              disabled: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </section>

  <section class="nhsuk-main-wrapper app-wrapper app-wrapper--background-grey">
    <div class="nhsuk-width-container">

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-two-thirds">
          <h2 class="nhsuk-heading-l nhsuk-u-margin-bottom-0">Grey background (used in footer)</h2>
        </div>
      </div>

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-one-third">
          <p class="nhsuk-body nhsuk-u-margin-top-6">Button element</p>

          <div class="nhsuk-button-group app-button-group">
            {{ button({
              text: "Find my location",
              variant: "secondary-solid"
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <p class="nhsuk-body nhsuk-u-margin-top-6">Link element</p>

          <div class="nhsuk-button-group app-button-group">
            {{ button({
              text: "Find my location",
              variant: "secondary-solid",
              href: "#"
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <p class="nhsuk-body nhsuk-u-margin-top-6">Disabled</p>

          <div class="nhsuk-button-group app-button-group">
            {{ button({
              text: "Find my location",
              variant: "secondary-solid",
              disabled: true
            }) }}
          </div>
        </div>
      </div>

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          <div class="nhsuk-button-group">
            {{ button({
              text: "Find my location",
              variant: "secondary-solid",
              small: true
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          <div class="nhsuk-button-group">
            {{ button({
              text: "Find my location",
              variant: "secondary-solid",
              small: true,
              href: "#"
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          <div class="nhsuk-button-group">
            {{ button({
              text: "Find my location",
              variant: "secondary-solid",
              small: true,
              disabled: true
            }) }}
          </div>
        </div>
      </div>

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "secondary-solid",
              icon: item.modifier,
              small: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "secondary-solid",
              icon: item.modifier,
              small: true,
              href: "#"
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "secondary-solid",
              icon: item.modifier,
              small: true,
              disabled: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </section>

  <section class="nhsuk-main-wrapper app-wrapper app-wrapper--background-blue">
    <div class="nhsuk-width-container">

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-two-thirds">
          <h2 class="nhsuk-heading-l nhsuk-u-margin-bottom-0">Blue background (used in hero)</h2>
        </div>
      </div>

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-one-third">
          <p class="nhsuk-body nhsuk-u-margin-top-6">Button element</p>

          <div class="nhsuk-button-group app-button-group">
            {{ button({
              text: "Log out",
              variant: "reverse"
            }) }}

            {{ button({
              text: "Find",
              variant: "reverse",
              icon: "search"
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <p class="nhsuk-body nhsuk-u-margin-top-6">Link element</p>

          <div class="nhsuk-button-group app-button-group">
            {{ button({
              text: "Log out",
              href: "#",
              variant: "reverse"
            }) }}

            {{ button({
              text: "Find",
              href: "#",
              variant: "reverse",
              icon: "search"
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <p class="nhsuk-body nhsuk-u-margin-top-6">Disabled</p>

          <div class="nhsuk-button-group app-button-group">
            {{ button({
              text: "Log out",
              variant: "reverse",
              disabled: true
            }) }}

            {{ button({
              text: "Find",
              variant: "reverse",
              icon: "search",
              disabled: true
            }) }}
          </div>
        </div>
      </div>

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          <div class="nhsuk-button-group">
            {{ button({
              text: "Log out",
              variant: "reverse",
              small: true
            }) }}

            {{ button({
              text: "Find",
              variant: "reverse",
              icon: "search",
              small: true
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          <div class="nhsuk-button-group">
            {{ button({
              text: "Log out",
              href: "#",
              variant: "reverse",
              small: true
            }) }}

            {{ button({
              text: "Find",
              href: "#",
              variant: "reverse",
              icon: "search",
              small: true
            }) }}
          </div>
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          <div class="nhsuk-button-group">
            {{ button({
              text: "Log out",
              variant: "reverse",
              small: true,
              disabled: true
            }) }}

            {{ button({
              text: "Find",
              variant: "reverse",
              icon: "search",
              small: true,
              disabled: true
            }) }}
          </div>
        </div>
      </div>

      <div class="nhsuk-grid-row">
        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "reverse",
              icon: item.modifier,
              small: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          <p class="nhsuk-body-s">Shadow removed using: <br><code class="nhsuk-code--inline nhsuk-code--reverse">.app-button--no-shadow</code></p>

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "reverse",
              classes: "app-button--no-shadow",
              icon: item.modifier,
              small: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "reverse",
              icon: item.modifier,
              small: true,
              href: "#"
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          <p class="nhsuk-body-s">Shadow removed using: <br><code class="nhsuk-code--inline nhsuk-code--reverse">.app-button--no-shadow</code></p>

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "reverse",
              classes: "app-button--no-shadow",
              icon: item.modifier,
              small: true,
              href: "#"
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}
        </div>

        <div class="nhsuk-grid-column-one-third">
          <hr class="nhsuk-section-break nhsuk-section-break--l nhsuk-section-break--visible">

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "reverse",
              icon: item.modifier,
              small: true,
              disabled: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}

          <hr class="nhsuk-section-break nhsuk-section-break--m nhsuk-section-break--visible">

          <p class="nhsuk-body-s">Shadow removed using: <br><code class="nhsuk-code--inline nhsuk-code--reverse">.app-button--no-shadow</code></p>

          {% for row in examples | batch(4) %}
          <div class="nhsuk-button-group nhsuk-button-group--small app-button-group app-button-group--nowrap">
            {% for item in row | select("mapping") %}
            {{ button({
              ariaLabel: item.name,
              variant: "reverse",
              classes: "app-button--no-shadow",
              icon: item.modifier,
              small: true,
              disabled: true
            }) | trim | indent(12) }}
            {% endfor %}
          </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </section>
{% endblock %}
