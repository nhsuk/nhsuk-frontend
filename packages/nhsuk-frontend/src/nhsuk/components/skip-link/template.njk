{% from "nhsuk/macros/attributes.njk" import nhsukAttributes %}

{#- Set classes for this component #}
{%- set classNames = "nhsuk-skip-link" -%}

{%- if params.classes %}
  {% set classNames = classNames ~ " " ~ params.classes %}
{% endif -%}

<a {%- if params.id %} id="{{ params.id }}"{% endif %} class="{{ classNames }}" href="{{ params.href | default("#maincontent", true) }}"
  {{- nhsukAttributes(params.attributes) }} data-module="nhsuk-skip-link">
{% if params.html %}
  {{ params.html | safe | trim | indent(2) }}
{% else %}
  {{ params.text | default("Skip to main content", true) }}
{% endif %}
</a>
