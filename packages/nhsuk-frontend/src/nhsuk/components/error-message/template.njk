{% from "nhsuk/components/content/macro.njk" import content as nhsukContent %}
{% from "nhsuk/macros/attributes.njk" import nhsukAttributes %}

{#- Set classes for this component #}
{%- set classNames = "nhsuk-error-message" -%}

{%- if params.classes %}
  {% set classNames = classNames ~ " " ~ params.classes %}
{% endif -%}

{%- set content = {
  id: params.id,
  text: params.text,
  html: params.html,
  visuallyHidden: {
    html: params.visuallyHiddenText | default("Error") ,
    placement: "start",
    suffix: ":"
  },
  classes: classNames,
  attributes: params.attributes,
  element: "span"
} %}

{%- if caller %}
  {%- set outerCaller = caller %}
  {%- call nhsukContent(content) %}
    {{- outerCaller() }}
  {%- endcall %}
{% else %}
  {{- nhsukContent(content) | trim }}
{% endif %}
