<nav class="nhsuk-pagination{%- if params.classes %} {{ params.classes }}{% endif %}"{% for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor %} role="navigation" aria-label="Pagination">
  {% if params.previousUrl and params.previousTitle %}
  <a href="{{ params.previousUrl }}" class="nhsuk-pagination-prev">
    <span class="nhsuk-pagination-link">Previous</span>
    <span class="nhsuk-pagination-title">{{ params.previousTitle }}</span>
    {% include 'assets/icons/icon-arrow-left.svg' %}
  </a>
  {% endif %}
  {% if params.nextUrl and params.nextTitle %}
  <a href="{{ params.nextUrl }}" class="nhsuk-pagination-next">
    <span class="nhsuk-pagination-link">Next</span>
    <span class="nhsuk-pagination-title">{{ params.nextTitle }}</span>
    {% include 'assets/icons/icon-arrow-right.svg' %}
  </a>
  {% endif %}
</nav>
