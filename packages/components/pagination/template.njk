<nav class="nhsuk-pagination{%- if params.classes %} {{ params.classes }}{% endif %}"{% for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor %} role="navigation" aria-label="Pagination">
  {% if params.previousUrl and params.previousPage %}
  <a href="{{ params.previousUrl }}" class="nhsuk-pagination__link nhsuk-pagination__link--prev">
    <span class="nhsuk-pagination__title">Previous</span>
    <span class="nhsuk-pagination__page">{{ params.previousPage }}</span>
    {% include 'assets/icons/icon-arrow-left.svg' %}
  </a>
  {% endif %}
  {% if params.nextUrl and params.nextPage %}
  <a href="{{ params.nextUrl }}" class="nhsuk-pagination__link nhsuk-pagination__link--next">
    <span class="nhsuk-pagination__title">Next</span>
    <span class="nhsuk-pagination__page">{{ params.nextPage }}</span>
    {% include 'assets/icons/icon-arrow-right.svg' %}
  </a>
  {% endif %}
</nav>
