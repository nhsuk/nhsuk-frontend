<div class="nhsuk-c-promo {%- if params.classes %} {{ params.classes }}{% endif %}"
{%- for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor %}>
  <a class="nhsuk-c-promo__link-wrapper"  href="{{ params.href }}">
    {% if params.imgURL %}
    <img class="nhsuk-c-promo__img" src="{{ params.imgURL }}" alt="{{ params.imgALT }}" />
    {% endif %}
    <div class="nhsuk-c-promo__content">
      <h3 class="nhsuk-c-promo__heading">{{ params.heading | safe }}</h3>
      {% if params.description %}
      <p class="nhsuk-c-promo__description">{{ params.description | safe }}</p>
      {% endif %}
    </div>
  </a>
</div>

