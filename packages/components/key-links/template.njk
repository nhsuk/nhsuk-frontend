<nav class="nhsuk-key-links
{%- if params.classes %} {{ params.classes }}{% endif %}"
{%- for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor %} role="navigation" aria-label="Pages in this section">
  <h2 class="nhsuk-u-visually-hidden">Contents</h2>
  <ul class="nhsuk-key-links__list">
    {%- for item in params.items %}
      <li class="nhsuk-key-links__item">
        <a class="nhsuk-key-links__link" href="{{ item.href }}">{{ item.text }}</a>
      </li>
    {%- endfor %}
  </ul>
</nav>
