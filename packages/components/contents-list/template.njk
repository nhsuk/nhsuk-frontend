<nav class="nhsuk-c-contents-list{%- if params.classes %} {{ params.classes }}{% endif %}"{% for attribute, value in params.attributes %} {{attribute}}="{{value}}"{% endfor %} aria-label="Pages in this guide">
  <h2 class="nhsuk-visually-hidden">Contents</h2>
  <ol class="nhsuk-c-contents-list__list">
    {% for item in params.items %}
    <li class="nhsuk-contents-list__item">
      {% if item.current %}
        <span class="nhsuk-contents-list__current" aria-current="page">{{ item.label }}</span>
        {% else %}
        <a class="nhsuk-contents-list__link" href="{{ item.URL }}">{{ item.label }}</a>
      {% endif %}
    </li>
    {% endfor %}
  </ol>
</nav>
