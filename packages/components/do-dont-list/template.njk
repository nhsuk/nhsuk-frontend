<div class="nhsuk-do-dont-list">
  <h3 class="nhsuk-do-dont-list__label">{{title}}</h3>
  {% if type == 'tick' %}
    <ul class="nhsuk-list nhsuk-list--check">
      {% for data in items %}
      <li>
      {% include 'assets/icons/icon-tick.svg' %}
      {{ data.item | safe}}
      </li>
      {% endfor %}
    </ul>
  {% elseif type == 'cross' %}
    <ul class="nhsuk-list nhsuk-list--cross">
    {% for data in items %}
      <li>
      {% include 'assets/icons/icon-cross.svg' %}
      don't {{ data.item | safe}}
      </li>
    {% endfor %}
    </ul>
  {% endif %}
</div>
