{% from "nhsuk/macros/attributes.njk" import nhsukAttributes %}

<div {%- if params.id %} id="{{ params.id }}"{% endif %} class="nhsuk-inset-text
{%- if params.classes %} {{ params.classes }}{% endif %}"
{{- nhsukAttributes(params.attributes) }}>
  <span class="nhsuk-u-visually-hidden">{{ params.visuallyHiddenText | default("Information", true) }}: </span>
  {% if caller or params.html -%}
    {{ caller() if caller else params.html | safe | trim | indent(2) }}
  {% elif params.text -%}
    <p>{{ params.text }}</p>
  {% endif %}
</div>
