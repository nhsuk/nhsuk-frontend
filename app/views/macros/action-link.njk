{% macro actionLink(url, text, class) %}
    <a href="{{url}}" class="nhsuk-c-action-link {{class}}">
        {% include 'partials/icons/icon-arrow-right-circle.svg' %}
        <span>{{text}}</span>
    </a>
{% endmacro %}
